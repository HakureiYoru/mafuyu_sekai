# v6.1 移动布局视觉检查

2026-09-18，生产入口 `assets/index--GSm3RKQ.js`。Playwright Chromium，触控与移动视口模拟、DPR 2，Windows / D3D11。不是 iOS 或 Android 真机性能证据。

检查 568×320、844×390、390×844 的菜单、设置、开场通讯；横屏另检查战斗、选卡滚动、转为竖屏后暂停。截图等待对话框入场动画结束后采集，并通过 `view_image` 实际查看。`*-rotate-paused.png` 的文件名前缀表示旋转前尺寸，图片本身为旋转后的竖屏尺寸。

- 最小横屏画布为 372×209.25 CSS px，保持 16:9。左侧摇杆、右侧四按钮、顶部/底部 HUD、通讯均在战区之外。冲刺为 60×66 CSS px，其余动作按钮最小边长不小于 44 CSS px。
- 568×320 通讯使用小横排双头像，全文气泡位于右侧按钮上方；开场截图中两者间有空隙。
- 设置和强化面板均可滚动；`*-upgrade-scroll.png` 展示到达底部的重抽、完整构筑入口和返回菜单按钮。
- 竖屏菜单禁用开局；横屏进入强化后旋转，完成最后一次选择会进入旋转暂停提示。此处通过实际点击检查，完整手势回归另见移动浏览器测试记录。
- `844x390-manual-lock.png` 通过点击 PALISADE 的侧臂检查冷色锁定标记。没有更改原人物贴图。
- 三个上下文均无 `pageerror`，几何采样见 `visual-check.json`。

为稳定查看布局，开场夹具给玩家临时无敌并停止新增敌人；PALISADE 使用既有调试练习入口。截图隐藏了调试性能面板。这些画面不用于证明自然战斗难度、性能或手机续航。浏览器与 5186 预览服务已关闭。
